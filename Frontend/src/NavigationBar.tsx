

const NavigationBar = () => {
    return (
        <nav className="navbar">
            <h1>Paws and claws</h1>
            <div className="links">
                <a href="/">Home</a>
                <a href="/create">logout</a>
            </div>
        </nav>
    );
};

export default NavigationBar;
